{
  "$schema": "../../schema/recipe.schema.json",
  "id": "ecwid",
  "kind": "platform",
  "name": "Ecwid (*.company.site storefronts)",
  "status": "partial",
  "cardSurface": "stripe-payment-element",
  "cvvTarget": "payment-frame:input[name=cvc]",
  "detect": {
    "hosts": [
      "*.company.site",
      "*.ecwid.com"
    ],
    "selectors": [
      "script[src*=\"app.ecwid.com\"]",
      ".ec-cart",
      "#productBrowser"
    ]
  },
  "gotchas": [
    "The Terms checkbox must be ticked before Continue enables — target only terms/agree checkboxes, never arbitrary ones."
  ],
  "notes": "Wizard flow mapped in the 2026-06 campaign; no receipt-verified purchase yet. First end-to-end drives landed 2026-07-20: onestopspiritualshoppe.company.site reaches the payment surface (level `cart`) and classifies correctly as paypal-only, while ratrodmagazinemerchandise.company.site stalls earlier at `detect` with 'could not reach checkout' — so detection is sound but add-to-cart is not yet reliable across Instant Sites.",
  "steps": {
    "addToCart": "'Add to Bag' → mini-cart → 'Go to Checkout'.",
    "billing": "Wizard: name/address after email; advance with 'Continue to Payment'.",
    "contact": "Email, plus a REQUIRED Terms checkbox that gates the Continue button.",
    "outcome": "Confirmation / decline text.",
    "payment": "Select the card payment method, then a Stripe frame. Card-number token, then the CVV token into the payment/CVC frame immediately after.",
    "placeOrder": "'Place order', once."
  }
}
